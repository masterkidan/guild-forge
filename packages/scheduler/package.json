{
  "name": "@guild-forge/scheduler",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@guild-forge/shared": "*",
    "node-cron": "^3.0.3",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
